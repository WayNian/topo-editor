// set code cofig
export enum CodeConfig {
  success = 200,
  notFound = 404,
  noPermission = 403
}

// set code cofig
export enum HttpCodeConfig {
  success = 200,
  notFound = 404,
  noPermission = 403
}
